# /src/@orb.yml
# yamllint disable rule:line-length
---
version: 2.1

description: |
  Orb for using vault secrets automation within your pipeline.

  Available commands include:

  - install: install vault cli binary

display:
  source_url: "https://github.com/twplatformlabs/orb-vault"
  home_url: "https://github.com/twplatformlabs/orb-vault"
